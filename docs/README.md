# docs

This directory documents behavior that is already implemented in code.

## Structure

- `core/` for cross-cutting runtime and platform usage.
- `realms/<realm>/...` for detailed packet and feature usage by realm.

## Rules

- Update `docs/` when implementation changes.
- Keep examples executable and aligned with current behavior.
- Avoid planning content here; planning belongs in `architecture/`.
