# services

Executable service modules composing pixel-server runtime.

- `gateway` websocket ingress/egress
- `auth` handshake and session authentication
- `game` ECS room simulation
- `social`, `navigator`, `catalog`, `moderation` domain services
